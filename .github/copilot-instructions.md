# Ghost Shield Workspace

- Backend: FastAPI under `backend/app`.
- Frontend: Vite React under `frontend`.
- Keep the MVP runnable without cloud credentials or research-only model weights.
- Use `pytest` for backend tests and `npm run build` for frontend validation.
- Do not commit secrets; use `.env.example` as the configuration template.
